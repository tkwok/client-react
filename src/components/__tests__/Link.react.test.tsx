import * as React from "react";
import Link from "../Link.react";
import * as renderer from "react-test-renderer";

test("Link changes the class when hovered", () => {
  let component = renderer.create(React.createElement("h1", null, "qwe"));
});
